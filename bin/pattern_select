#!/usr/bin/env bash
# Wrapper script for tmux-pick
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

UV_CMD="uv"
command -v uv &>/dev/null || UV_CMD="$HOME/.cargo/bin/uv"

exec "$UV_CMD" run --directory "$SCRIPT_DIR" -m tmux_pick "$@"
